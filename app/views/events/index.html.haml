#title-image
  %br/
  %h1.landing-page-text
    All The Swing Dancing!
.container
  .col-xs-8
    .about-info
      %hr
      Find All the Swing Dancing in one place
    .p{:style => "text-align: center; font-size: 18px;"}
      Welcome to the user-driven dance event posting site
    .p{:style => "text-align: center; font-size: 18px;"}
      keeping all your events in one handy location.
    .p{:style => "text-align: center; font-size: 18px; font-weight: 700;"}
      Always know when the dancing is happening!
      %hr
  .col-xs-4
    = image_tag("alltheswingdancing2x500.png", class: "img-responsive")
.section-header
  .container
    .row
      %h1.title-section All the Dance Events

.container
  .col-xs-3
    .search-title
      Search by Event Name
      = bootstrap_form_for events_path, :method => 'get' do
        = text_field_tag :search, params[:search]
        = submit_tag "Search", :name => nil
    .advertising-space
    .advertising-space
    / ADVERT SPACE HERE
  .col-xs-9
    = render partial: "panel_tile"

  / Resposive maps via GOOGLE
.section-header
  %h1.title-section
    All Event Locations
.container
  %div{:style => "width: 400px;"}
    #map{:style => "width: 950px; height: 400px;"}
  :javascript
    handler = Gmaps.build('Google');
    handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
      markers = handler.addMarkers(#{raw @hash.to_json});
      handler.bounds.extendWith(markers);
      handler.fitMapToBounds();
    });
  / END
