.container
  %h1#web-app-title{:style => "font-size:42px; text-align:center;"} All Events
  %hr/
  = render partial: "panel_tile"
  %br/
  %br/
  / Resposive maps via GOOGLE
  %h1#web-app-title{:style => "font-size:42px; text-align:center;"} All Events Map
  %hr/
  %div{:style => "width: 400px;"}
    #map{:style => "width: 950px; height: 400px;"}
  :javascript
    handler = Gmaps.build('Google');
    handler.buildMap({ provider: {}, internal: {id: 'map'}}, function(){
      markers = handler.addMarkers(#{raw @hash.to_json});
      handler.bounds.extendWith(markers);
      handler.fitMapToBounds();
    });
  / END
